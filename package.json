{
  "name": "maparescate",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "app",
    "api"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "ci": "npm run lint --workspaces && npm run test --workspace api && npm run test --workspace app && npm run e2e:mocked --workspace app && npm run build --workspace app",
    "import:reports": "node scripts/import-reports.mjs",
    "admin:moderate": "node scripts/admin-moderate.mjs",
    "smoke:launch": "node scripts/launch-smoke.mjs",
    "test": "npm run test --workspaces"
  }
}
